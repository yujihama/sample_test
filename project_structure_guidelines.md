# プロジェクト構造ガイドライン

## 目的

このドキュメントは、プロジェクト構造の一貫性を維持し、冗長なフォルダやファイルの作成を防止するためのガイドラインを提供します。すべての開発者はこのガイドラインに従ってください。

## 基本方針

1. **シンプルな構造を維持する**: 不必要な階層や重複したディレクトリを作成しない
2. **標準に従う**: 各技術スタックの標準的なディレクトリ構造に準拠する
3. **関連性を重視する**: 関連するファイルは適切なディレクトリにまとめる
4. **明確な命名**: ディレクトリやファイルの名前は、その内容や目的を明確に反映する

## ディレクトリ構造

### ルートディレクトリ

ルートディレクトリには以下のディレクトリとファイルのみを配置してください：

```
/
├── backups/             # バックアップデータ（日付ごとにサブディレクトリ化）
├── config/              # 設定ファイル
├── data/                # アプリケーションデータ
├── docs/                # プロジェクトドキュメント
├── logs/                # ログファイル
├── migrations/          # データベースマイグレーション (Alembic)
├── src/                 # ソースコード
├── static/              # 静的ファイル
├── tests/               # テストコード
├── uploads/             # アップロードされたファイル
├── venv/                # 仮想環境（バージョン管理対象外）
├── .gitignore           # Gitの無視ファイル設定
├── README.md            # プロジェクト概要
├── requirements.txt     # Pythonの依存関係
└── project_structure_guidelines.md  # 本ガイドライン
```

**重要**: 上記以外のディレクトリをルートに作成する場合は、チームでの検討と承認が必要です。

### 各ディレクトリの役割と規則

#### src/ - ソースコード

アプリケーションのソースコードはすべて `src/` ディレクトリに配置します：

```
src/
├── api/                # API関連のコード
├── core/               # コアモジュール
├── db/                 # データベース関連
├── models/             # データモデル
├── repositories/       # リポジトリパターン実装
├── scripts/            # 運用・管理スクリプト
├── services/           # サービス層
├── tools/              # ユーティリティツール
├── utils/              # 汎用ユーティリティ
├── main.py             # アプリケーションのエントリポイント
└── __init__.py         # Pythonパッケージ定義
```

#### docs/ - ドキュメント

ドキュメントは適切なカテゴリごとにサブディレクトリに整理します：

```
docs/
├── api/                # API仕様書
├── architecture/       # アーキテクチャ設計書
├── database/           # データベース設計書
├── development/        # 開発ガイド
├── testing/            # テスト関連ドキュメント
└── user_guide/         # ユーザーガイド
```

#### tests/ - テスト

テストコードは機能や種類ごとに整理します：

```
tests/
├── api/                # APIテスト
├── core/               # コアモジュールテスト
├── db_integration/     # データベース統合テスト
├── unit/               # ユニットテスト
└── conftest.py         # Pytestの共通設定
```

#### backups/ - バックアップ

バックアップは日付ごとにサブディレクトリ化します：

```
backups/
└── YYYYMMDD/           # 日付ごとのディレクトリ
    ├── data/           # データのバックアップ
    ├── migrations/     # マイグレーションのバックアップ
    └── ...
```

## ファイル配置ルール

1. **設定ファイル**:
   - アプリケーション設定は `config/` ディレクトリに配置
   - 環境変数は `.env` ファイルで管理（バージョン管理対象外）

2. **スクリプト**:
   - 運用・管理スクリプトは `src/scripts/` ディレクトリに配置
   - テスト用スクリプトは `tests/` 内の適切なディレクトリに配置

3. **ドキュメント**:
   - すべてのドキュメントは `docs/` ディレクトリ内に配置
   - READMEファイルはそれぞれのディレクトリで許可

4. **ログ**:
   - ログファイルは `logs/` ディレクトリに出力
   - ログファイルはバージョン管理対象外

## 命名規則

1. **ディレクトリ名**:
   - すべて小文字
   - スネークケース（単語の間にアンダースコア）
   - 複数形を使用（例: `models/`, `services/`）

2. **ファイル名**:
   - Pythonファイル: スネークケース（例: `user_service.py`）
   - クラス定義ファイル: クラス名と一致（スネークケース）
   - テストファイル: `test_` プレフィックス（例: `test_user_service.py`）

## 新しいモジュールの追加

1. 新しいモジュールを追加する前に、既存のディレクトリ構造内に適切な場所があるか確認する
2. 機能的に関連するコードは同じディレクトリにまとめる
3. 必要に応じてサブディレクトリを作成する
4. 同様の機能やツールが別の場所に重複して存在しないことを確認する

## 特別なケース

1. **フレームワーク固有のディレクトリ構造**:
   - フレームワーク（Flask, Django, FastAPIなど）が特定のディレクトリ構造を要求する場合は、それに従う
   - ただし、可能な限り `src/` ディレクトリ内に組み込む

2. **サードパーティライブラリ**:
   - サードパーティのライブラリやパッケージは `venv/` にインストール
   - ライブラリに付属するスクリプトやツールはそのまま使用し、コピーしない

3. **マイグレーション**:
   - Alembicなどのマイグレーションツールは標準的な構成に従い、`migrations/` ディレクトリをルートに配置

## メンテナンス

1. **定期的な整理**:
   - 四半期ごとにプロジェクト構造をレビューし、このガイドラインへの準拠を確認
   - 不要なファイルやディレクトリを特定し、削除または移動

2. **バックアップポリシー**:
   - バックアップを作成する際は、`backups/YYYYMMDD/` 形式のディレクトリに配置
   - 古いバックアップは定期的に削除または圧縮

## ガイドライン違反の例

以下のような行為は避けてください：

1. ルートディレクトリに直接スクリプトファイルを作成する
2. 同じ機能のコードを複数の場所に散在させる
3. 類似した名前の複数のディレクトリを作成する（例: `tools/` と `utilities/`）
4. 一時的なファイルをバージョン管理に含める
5. 使われなくなったコードやファイルをそのまま残す

## 変更手順

このガイドラインを変更する必要がある場合は、以下の手順に従ってください：

1. 変更提案をチームに共有
2. 変更の影響範囲を評価
3. チームの合意を得る
4. 本ドキュメントを更新
5. 必要に応じてプロジェクト構造を調整

---

**最終更新**: 2025年3月17日 