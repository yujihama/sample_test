# 内部監査サンプルデータ自動テストAIエージェント 修正計画

## 概要
現在のプロジェクトには以下の3つの主要な問題があります：
1. ブラウザのコンソールで表示されるエラーをログファイルに出力できていない
2. ほとんどの機能でフロントエンドがバックエンドから取得した情報を表示できていない
3. フロントエンドのコードが散在しており体系だっていない

以下、それぞれの問題に対する具体的な修正計画を示します。

## 修正方針の重要事項
- **バックエンドよりフロントエンドを優先**: バックエンドの仕様は既に整備されており、テストも通っているため、既存のAPIインターフェースを尊重し、フロントエンド側の実装を修正する方針で対応します。不必要なバックエンドの変更は避けるようにします。
- **検証環境**: Windows環境でのPowerShellを使用して各種コマンドを実行します。
- **継続的なテスト**: 各修正フェーズの節目でtestsフォルダ配下のsmokeテストをパスすることを確認し、デグレーションを防止します。

## 1. ブラウザのコンソールエラーのログファイル出力

### 現状の問題
- フロントエンドで発生するエラーは現在ブラウザのコンソールにのみ表示され、サーバーサイドのログファイルに記録されていない
- APIリクエスト失敗時に`console.warn()`や`console.error()`でログを出力しているが、これらは永続化されていない
- バックエンドとの連携が取れておらず、クライアント側で発生する問題の追跡が困難

### 修正計画

#### 1.1 フロントエンドログ収集APIの実装
```javascript
// 新規作成: src/frontend/services/logger.js
import { apiClient } from './api';

export const loggerService = {
  // フロントエンドエラーをバックエンドに送信
  logError: (error, context = {}) => {
    return apiClient.post('/api/v1/logs/client', {
      level: 'error',
      message: error.message || 'Unknown error',
      stack: error.stack,
      context: { ...context, userAgent: navigator.userAgent }
    }).catch(err => {
      // APIが失敗してもコンソールには表示
      console.error('エラーログの送信に失敗:', err);
    });
  },
  
  // 警告ログの送信
  logWarning: (message, context = {}) => {
    return apiClient.post('/api/v1/logs/client', {
      level: 'warning',
      message,
      context: { ...context, userAgent: navigator.userAgent }
    }).catch(err => {
      console.warn('警告ログの送信に失敗:', err);
    });
  },
  
  // 情報ログの送信
  logInfo: (message, context = {}) => {
    return apiClient.post('/api/v1/logs/client', {
      level: 'info',
      message,
      context: { ...context, userAgent: navigator.userAgent }
    }).catch(err => {
      console.info('情報ログの送信に失敗:', err);
    });
  }
};
```

#### 1.2 グローバルエラーハンドリングの実装
```javascript
// 新規作成: src/frontend/error/ErrorBoundary.js
import React from 'react';
import { loggerService } from '../services/logger';

class ErrorBoundary extends React.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false, errorInfo: null };
  }

  static getDerivedStateFromError(error) {
    return { hasError: true };
  }

  componentDidCatch(error, errorInfo) {
    this.setState({ errorInfo });
    
    // エラー情報をバックエンドに送信
    loggerService.logError(error, {
      componentStack: errorInfo.componentStack,
      componentName: this.props.componentName || 'Unknown'
    });
  }

  render() {
    if (this.state.hasError) {
      return this.props.fallback || <h2>エラーが発生しました。開発チームに報告されました。</h2>;
    }
    return this.props.children;
  }
}

export default ErrorBoundary;
```

#### 1.3 バックエンドでのクライアントログ処理API
```python
# 新規作成: src/api/routers/log_endpoints.py
from fastapi import APIRouter, Body
from pydantic import BaseModel
from typing import Dict, Any, Optional
from loguru import logger
import os
from src.core.config import settings

router = APIRouter()

class ClientLogEntry(BaseModel):
    level: str
    message: str
    context: Dict[str, Any] = {}
    stack: Optional[str] = None

@router.post("/client")
async def log_client_error(log_entry: ClientLogEntry = Body(...)):
    """クライアントからのログを受け取り、サーバーのログファイルに記録する"""
    
    # クライアントログ専用のファイル
    client_log_path = os.path.join(settings.LOGS_DIR, "client.log")
    
    # コンテキスト情報を文字列化
    context_str = ", ".join([f"{k}={v}" for k, v in log_entry.context.items()])
    
    # ログレベルに応じた処理
    log_message = f"CLIENT: {log_entry.message} | {context_str}"
    
    if log_entry.level.lower() == "error":
        logger.error(log_message)
        if log_entry.stack:
            logger.error(f"Stack trace: {log_entry.stack}")
    elif log_entry.level.lower() == "warning":
        logger.warning(log_message)
    else:
        logger.info(log_message)
    
    return {"status": "success"}
```

#### 1.4 ログルーターをAPIに追加
APIのmain.pyファイルにログルーターを追加する必要があります。

#### 1.5 サービスのログ出力を修正
既存のAPI呼び出しでエラーハンドリングにloggerServiceを使用するよう修正します。

## 2. フロントエンドとバックエンドの連携改善

### 現状の問題
- APIからのデータ取得に失敗した際にフォールバックとしてモックデータを使用しているが、エラー原因が不明確
- バックエンドAPIの応答形式とフロントエンドの期待する形式に不一致がある
- エラー表示や読み込み状態の処理が不十分

### 修正計画

#### 2.1 API応答形式の統一化
バックエンドAPIとフロントエンドの連携を改善するため、応答形式を統一します。バックエンドAPIの仕様に合わせてフロントエンド側を調整します。

```javascript
// 修正: src/frontend/services/api.js のインターセプター部分
// レスポンスインターセプターの追加
apiClient.interceptors.response.use(
  (response) => {
    // 成功時の処理
    return response;
  },
  (error) => {
    // エラーログの送信
    if (error.response) {
      // サーバーからのレスポンスがある場合
      loggerService.logError(
        new Error(`API Error: ${error.response.status} ${error.response.statusText}`),
        {
          url: error.config.url,
          method: error.config.method,
          responseData: error.response.data
        }
      );
    } else if (error.request) {
      // レスポンスを受け取れなかった場合
      loggerService.logError(
        new Error('ネットワークエラー: サーバーからの応答がありません'),
        {
          url: error.config.url,
          method: error.config.method
        }
      );
    } else {
      // リクエスト設定中のエラー
      loggerService.logError(error, {
        message: 'リクエスト設定エラー',
        config: error.config
      });
    }
    
    return Promise.reject(error);
  }
);
```

#### 2.2 API呼び出しのカスタムフック改善
useApiフックを改善し、より使いやすく一貫性のあるデータ取得を実現します。

```javascript
// 修正: src/frontend/hooks/useApi.js
import { useState, useEffect, useCallback } from 'react';
import { loggerService } from '../services/logger';

const useApi = (apiFunction, initialArgs = [], initialData = null, autoFetch = true) => {
  const [data, setData] = useState(initialData);
  const [loading, setLoading] = useState(autoFetch);
  const [error, setError] = useState(null);
  const [lastFetchTime, setLastFetchTime] = useState(null);

  const fetchData = useCallback(async (...args) => {
    // 引数が指定されない場合は初期値を使用
    const actualArgs = args.length > 0 ? args : initialArgs;
    
    setLoading(true);
    setError(null);
    
    try {
      const response = await apiFunction(...actualArgs);
      setData(response.data);
      setLastFetchTime(new Date());
      return response.data;
    } catch (err) {
      const errorMessage = err.response?.data?.message || err.message || '不明なエラー';
      setError(errorMessage);
      
      // エラーはすでにインターセプターで処理されているので、ここでは追加処理しない
      return null;
    } finally {
      setLoading(false);
    }
  }, [apiFunction, initialArgs]);

  useEffect(() => {
    if (autoFetch) {
      fetchData();
    }
  }, [fetchData, autoFetch]);

  return {
    data,
    loading,
    error,
    refetch: fetchData,
    lastFetchTime
  };
};

export default useApi;
```

#### 2.3 コンポーネント内でのデータ連携改善
コンポーネント内でのAPI連携部分を改善し、データ取得状態をより明確に表示します。バックエンドAPIの応答形式に合わせてコンポーネントを調整します。

## 3. フロントエンドコードの構造改善

### 現状の問題
- フロントエンドのコードが`src/components`、`src/frontend`、`src/App.js`などに散在している
- ディレクトリ構造に一貫性がなく、関連するファイルが離れた場所に配置されている
- コードの再利用性が低く、似たようなコードの重複がある

### 修正計画

#### 3.1 フロントエンドディレクトリ構造の再編成
フロントエンドのコードをすべて`src/frontend`に移動し、論理的な構造に再編成します。

```
src/
├── frontend/           # フロントエンドコードのルート
│   ├── components/     # UIコンポーネント
│   │   ├── common/     # 共通コンポーネント
│   │   ├── layout/     # レイアウト関連コンポーネント 
│   │   ├── dashboard/  # ダッシュボード関連
│   │   ├── agents/     # エージェント関連
│   │   └── ...         # その他機能別コンポーネント
│   ├── hooks/          # カスタムReactフック
│   ├── services/       # APIサービス
│   ├── utils/          # ユーティリティ関数
│   ├── context/        # Reactコンテキスト
│   ├── assets/         # 静的アセット（画像など）
│   ├── App.js          # ルートコンポーネント
│   ├── index.js        # エントリーポイント
│   └── index.css       # グローバルスタイル
└── ...                 # バックエンド関連ディレクトリ
```

#### 3.2 コンポーネントの再構築
- 現在のコンポーネントを新しい構造に合わせて移動・リファクタリング
- 共通部分を抽出して再利用可能なコンポーネントを作成
- コンポーネント間の依存関係を明確化

#### 3.3 Webpack設定の更新
ディレクトリ構造変更に伴いWebpack設定を更新し、エイリアスを設定して相対パスの複雑さを軽減します。

```javascript
// webpack.config.js に追加
resolve: {
  alias: {
    '@frontend': path.resolve(__dirname, 'src/frontend/'),
    '@components': path.resolve(__dirname, 'src/frontend/components/'),
    '@services': path.resolve(__dirname, 'src/frontend/services/'),
    '@hooks': path.resolve(__dirname, 'src/frontend/hooks/'),
    '@utils': path.resolve(__dirname, 'src/frontend/utils/')
  }
}
```

## 実装スケジュール

1. **フェーズ1 - ログ出力システムの改善** (1-2日)
   - クライアントログ送信サービスの実装
   - バックエンドでのログエンドポイント追加
   - エラーバウンダリーの実装
   - PowerShellで以下のコマンドを実行してスモークテスト: `cd .\tests\ ; npm run smoke-test`

2. **フェーズ2 - API連携の改善** (2-3日)
   - APIサービスのリファクタリング
   - レスポンスインターセプターの追加
   - カスタムフックの改善
   - PowerShellで以下のコマンドを実行してスモークテスト: `cd .\tests\ ; npm run smoke-test`

3. **フェーズ3 - フロントエンド構造の再編成** (3-4日)
   - ディレクトリ構造の再編成
   - コンポーネントの移動とリファクタリング
   - Webpack設定の更新
   - PowerShellで以下のコマンドを実行してスモークテスト: `cd .\tests\ ; npm run smoke-test`

## 注意点
- リファクタリング中も既存の機能は維持する必要があります
- 段階的に変更を加え、各フェーズで動作確認を行います
- 実装の節目ごとにtestsフォルダ配下のsmokeテストを実行し、デグレーションを防止します
- PowerShellでコマンドを実行する際の例：
  ```powershell
  # アプリケーションの実行
  npm run dev
  
  # テストの実行
  cd .\tests\
  npm run smoke-test
  
  # バックエンドAPIの起動（必要な場合）
  cd .\src\
  python -m main
  ```
- バックエンドの変更は最小限に抑え、既存のAPI仕様に合わせてフロントエンド側を適応させることを優先します
- 自動テストを追加し、リグレッションを防止します 